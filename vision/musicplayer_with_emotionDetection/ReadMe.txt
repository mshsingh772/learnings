To Test the code, activate conda environment with libraries mentioned in requirements.txt
In prompt type 
    python test.py

Based on the random image selected from the images folder, the emotion is predicted.
Based on the predicted emotion, corresponding music is played.

To Train, download FER dataset, and place the unzipped folder in same directory in a folder named data.